{% extends 'base.html.twig' %}

{% block title %}Étudiant{% endblock %}

{% block body %}
    <h1>Étudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>NomEleve</th>
                <td>{{ etudiant.NomEleve }}</td>
            </tr>
            <tr>
                <th>PrenomEtudiant</th>
                <td>{{ etudiant.PrenomEtudiant }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_etudiant_index') }}">retour</a>

    <a class="btn btn-primary" href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}">éditer</a>

    {{ include('etudiant/_delete_form.html.twig') }}
{% endblock %}
